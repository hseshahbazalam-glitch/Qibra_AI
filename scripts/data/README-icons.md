# Icon-name metadata used by battery gate G6c.
#
# flutter_icons_stable.txt  = every 'static const IconData' name in
#   flutter/flutter packages/flutter/lib/src/material/icons.dart @ stable
#   (8825 names). THE authoritative set for Icons.X — it includes the
#   Flutter variant suffixes _rounded/_outlined/_sharp/_two_tone.
#   Source: https://api.github.com/repos/flutter/flutter/contents/
#     packages/flutter/lib/src/material/icons.dart?ref=stable
#
# material_icons_npm.txt     = base codepoint names from npm
#   material-icons@1.13.14 css/_codepoints.scss (2276 names, fetched via
#   registry.npmjs.org — the same egress path used for the Inter TTFs).
#   Base-only: no variant suffixes, so it is NOT sufficient for Flutter
#   validation (250 false positives repo-wide). Kept as a second opinion:
#   a name absent from BOTH sets is unambiguously not a Material icon.
#
# Regenerate:  see header of each fetch in the Stage E battery commit msg.
